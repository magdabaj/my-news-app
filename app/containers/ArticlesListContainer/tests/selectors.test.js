// import { selectArticlesListContainerDomain } from '../selectors';

describe("selectArticlesListContainerDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
