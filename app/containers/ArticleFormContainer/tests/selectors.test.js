// import { selectArticleFormContainerDomain } from '../selectors';

describe("selectArticleFormContainerDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
